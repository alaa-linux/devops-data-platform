apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: devops-data-platform
  name: readonly-role

rules:
  - apiGroups: [""]
    resources:
      - pods
      - services
      - configmaps
    verbs:
      - get
      - list
      - watch
